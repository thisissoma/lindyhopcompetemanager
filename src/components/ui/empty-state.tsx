export function EmptyState({ children }: { children: React.ReactNode }) {
  return <p className="rounded-[10px] border border-dashed border-border p-6 text-center text-sm text-muted">{children}</p>;
}
